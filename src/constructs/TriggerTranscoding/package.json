{
  "name": "trigger-video-transcoding",
  "version": "1.0.0",
  "description": "An AWS Lambda handler for triggering video transcoding",
  "main": "TriggerTranscodingFunction.TriggerTranscodingFunction.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/uuid": "^8.3.4",
    "jsonwebtoken": "^8.5.1",
    "uuid": "^8.3.4"
  },
  "workspaces": {
    "nohoist": [
      "**/**"
    ]
  }
}
